# Vendored evaluator

`agent_eval.py` is copied from:

```text
Workspace-Bench/evaluation/src/agent_eval.py
```

It is included so that `standalone_judge` can run the same native
Workspace-Bench rubric Judge without Docker or a second source checkout.
When the upstream evaluator changes, review and refresh this copy explicitly.
